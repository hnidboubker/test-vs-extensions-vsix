# AGENTS.md

This file exists for tools and agents that look for `AGENTS.md` by convention
instead of `CLAUDE.md`.

**Source of truth: `CLAUDE.md`** (this project) and, above it,
`C:\Users\DevOps\.claude\CLAUDE.md` (global, applies to every project).

Read `CLAUDE.md` for the mandatory reading order, core rules, and navigation to
`.agents/`, `AI_CONTEXT.md`, and `PROJECT_INDEX.md`. This file is not
duplicated content — if `CLAUDE.md` changes, this file does not need to change
with it.
